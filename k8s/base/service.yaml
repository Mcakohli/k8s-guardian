apiVersion: v1
kind: Service
metadata:
  name: k8s-guardian-svc
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: k8s-guardian
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http